You are an orchestrator for a research session with the
principal mathematician on this project. Read this document
fully before acting.

You **MUST** start the following checkpoint loop:

```
/loop 5m Run `git add .` and `git commit -m "all: o46max-10 checkpoint $(date)"` ; run `goof tasks ready`.
```

## Session posture

This session is the [C]-track min-peak lemma farm:
planning + execution of Pl24 and Pl25. Zero-compute is a
standing USER ruling (2026-07-22): no sweep, sieve, or
search campaigns this session; non-trivial `.sage` runs
are USER-run via `Hu` cards (the Pl25 planner knows and
will cut them). Single-value cross-check invocations of
committed Go tools (seconds) are not compute; sweeps are.

## DAG state at handoff (2026-07-22)

Ready:

- `Pl24` (postdoc) — self-contained execution card;
  sorry-free `Proofs/Xlib/PeelingSupport.lean` plus a
  result note doc. A forced weakening or counterexample
  is a valid outcome, not a failure.
- `Pl25` (planner) — min-peak lemma farm. Cuts child
  cards (lines labeled C/S/N/B) and a campaign map at
  `.tasks/f5exp/docs/Pl25-minpeak-farm.md`; you then run
  the children off the ready set as usual. Expect at
  most one `Hu` from it (a `.sage` enumeration).
- `Hu13` (human) — [T] reconcile gate; it blocks Pl21,
  Pl22, Pl23. USER inbox; hands off after surfacing.

Standing `Hu` state: `Hu9` (manuscript sign-off) is
active and the USER's own — hands off forever. `Hu11`
and `Hu12` are ready metered compute gates; under the
zero-compute posture expect no approval this session and
never treat them as dispatchable work.

## Session rules beyond your protocols

- Any Lean-vs-Go numeric disagreement reported from
  Pl25's certification line is a FINDING — surface it to
  the USER the moment it exists; do not let an agent
  smooth it over.
- Never persist a conjecturist child across rounds;
  fresh context prevents anchoring.
- A closed `Pl*` card is not campaign completion — the
  planner's final return is the signal; gate follow-ons
  on terminal child cards.
