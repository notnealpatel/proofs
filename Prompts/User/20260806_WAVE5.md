# WAVE 5 — covering arc, unblocked sketch discharge, and new-target follow-ons

Usage: give this file to a fresh ORCHESTRATOR context launched with
`--append-system-prompt="$(cat Prompts/System/20260806.md)"`. The
orchestrator dispatches each section verbatim as one lane brief.
Write targets are disjoint. Lane conduct is carried by
`.claude/agents/`, not by these briefs.

Read `NEXT_TARGETS.md` before dispatching — it has the full
prioritized queue with sequencing dependencies and wave-3 status
changes. Struck-through items are landed.

Constraints common to EVERY lane below — the orchestrator prepends
this block to each dispatched brief:

> Write only the file(s) this brief names. Axioms stay within
> {propext, Classical.choice, Quot.sound}; no `native_decide` unless
> this brief grants it; end the file with a `#print axioms` block for
> its main declarations. Pin every source claim VERBATIM from the
> primary source (`goof oeis show`, `goof erdos`, fetched papers into
> ./References) in the file header; if the source disagrees with this
> brief, halt and report the discrepancy. Scratch experiments go in
> `Proofs/Scratch/` and are removed with `goof rm` before you finish.
> Wrap `sage`/`python` in `timeout 300`; probe `command -v sage`
> first and disclose any sympy fallback. Do NOT post to GitHub, Slack,
> or any external system. Your final report: files and public
> declarations with statements, verbatim final build output,
> `#print axioms` results, every deviation, open items; then state
> the lane is ready for review.

When dispatching >2 concurrent lanes, tell agents to use
`timeout 60 lake build <module>` (or `timeout 300` for heavy decide
files) instead of `flock .lake/agent.lock lake build`.

---

## COVERING ARC — C7 + C3 (first priority)

### C7 (postdoc) — drop `1 ≤ k` from erdos_1950
(edits to `Proofs/Erdos/Covering/NotTwoPowerPlusPrime.lean`)

**Source:** PLAN.md C7.

**Goal.** Remove the `1 ≤ k` hypothesis from `erdos_1950_not_two_pow_add_prime`
and the bespoke `not_prime_sub_two_pow`. The k = 0 case is already
paid for: the covering class 0 (mod 2) with p = 3 gives 3 | m - 2^0
(docstring on `exists_mem_erdosPrimes1950_dvd` says so explicitly),
and the general-criterion rederivation `not_prime_sub_two_pow_of_general`
already dropped the hypothesis.

~15 lines of deletions. The strengthened flagship matches A006285's
k ≥ 0 convention.

Read the file first; find every use-site of `1 ≤ k` and assess
whether each can be dropped.

---

### C3 (postdoc) — Brier numbers
(new file `Proofs/Erdos/Covering/Brier.lean`)

**Source:** PLAN.md C3.

**Goal.** Prove that k = 3316923598096294713661 (Clavier) is
simultaneously Sierpiński and Riesel — a Brier number. This is TWO
independent `IsFixedDivisorSystem` instances using the certificates
re-verified 2026-08-05 in PLAN.md:

Sierpiński (p | k*2^a + 1), L = 48, 7 triples, max 108 bits:
  {(1,2,3),(2,4,5),(4,12,13),(0,8,17),(36,48,97),
   (20,24,241),(12,48,673)}

Riesel (p | k*2^a - 1), L = 180, 13 triples, max 109 bits:
  {(0,2,3),(0,3,7),(9,10,11),(11,18,19),(1,5,31),(23,36,37),
   (7,20,41),(10,60,61),(8,9,73),(5,36,109),(13,15,151),
   (25,30,331),(37,60,1321)}

Shape: mirroring the landed Sierpinski/Riesel files. The record k is
also the smallest known — Clavier's k is ~10^21, far below ACL2's five
(Cowles-Gamboa 2011). Value framing: first LEAN formalization + the
RECORD (smallest known) Brier k. Cite Cowles-Gamboa; claim no more.

Read `Sierpinski.lean`, `Riesel.lean`, `FixedDivisor.lean` first.

---

## UNBLOCKED BY WAVE 3 — Melfi discharge + Kummer use

### P15 (postdoc) — A005153 Switkay practical conjecture
(new file `Proofs/Enumerative/SwitkayPractical.lean`)

**Source:** NEXT_TARGETS F1, now S effort (Melfi sorry is an import).

**Goal.** Land the Switkay practical-number conjecture card. The sketch
`Proofs/Scratch/Candidates/A005153Switkay.lean` has the statement.
Import `Enumerative.MelfiPracticalSum` to discharge the `melfi` sorry.
The parity reduction (even practical → Zumkeller) should be provable.
Pin from `goof oeis show A005153`.

Read `Practical.lean`, `MelfiPracticalSum.lean`, and the sketch first.

---

### P16 (prover) — defect exact identity
(edits to `Proofs/Erdos/Erdos1063/DefectCarryBound.lean`)

**Source:** NEXT_TARGETS new-targets §1 (wave 3 finding).

**Goal.** Strengthen `badIndex_factorization_add_le` from `≤` to `=`:
`v_p(n - e) = v_p(C(n,k)) + v_p(k) + v_p(C(k-1,e))` at defect-1
points. The file's docstring already names this identity. Verified at
1162+ points, zero failures. The missing step is the ultrametric case
split (reversing the inequality).

~30-50 lines. P1-grade. Do NOT attempt the prime-k n_k strengthening
(structural dead end per elaborator).

Read `DefectCarryBound.lean` thoroughly before writing.

---

### P17 (postdoc) — Erdős #376 digit equivalence (A030979)
(new file `Proofs/Erdos/Erdos175/CoprimeCentralBinom.lean`)

**Source:** NEXT_TARGETS new-targets §2.

**Goal.** Prove: C(2n,n) is coprime to 105 iff n has base-3 digits ≤ 1,
base-5 digits ≤ 2, and base-7 digits ≤ 3. Three instantiations of the
landed `prime_not_dvd_centralBinom_iff_digits` at p = 3, 5, 7. This
discharges 4 of 6 sorrys in `E376.lean`. The $1000 infinitude stays
open (sorry or Prop def).

Pin from `goof erdos fetch 376` and `goof oeis show A030979`.

---

### P18 (postdoc) — A222603 practical tree
(new file `Proofs/Enumerative/PracticalTree.lean`)

**Source:** NEXT_TARGETS F3, now M effort (was L; 3 sorrys newly provable
from Melfi).

**Goal.** Land the practical-tree card. The sketch
`Proofs/Scratch/Candidates/A222603PracticalTree.lean` has the
statement. Import `MelfiPracticalSum` to discharge the Melfi sorry.
Acyclicity should be provable; connectivity is the open conjecture
(sorry). Successor well-definedness uses Melfi.

Read the sketch and `Practical.lean` first.

---

## COVERING ARC — C4 (second priority, M with L risk)

### C4 (postdoc) — Mirsky-Newman theorem
(new file `Proofs/Erdos/Covering/MirskyNewman.lean`)

**Source:** PLAN.md C4.

**Goal.** Prove: no disjoint distinct covering system exists (Mirsky-Newman /
Davenport-Rado; special case of Herzog-Schonheim). Root-of-unity
argument, stated directly on the committed `IsCoveringSystem`.

MUST resolve the 274.lean "Jostamon" pointer before any novelty claim
— read the upstream file at
`formal-conjectures/FormalConjectures/ErdosProblems/274.lean` (or
fetch it) and check what "Jostamon" refers to.

Strongest classical target in reach. First-formalization candidate
(no Mirsky-Newman / Davenport-Rado proof found in any ITP).

Read `Proofs/Erdos/Covering/Basic.lean` first.

---

## ARCHIVE BATCH (postdoc, parallel — disjoint files)

### Erdős #1213 Hegyvári
(new file `Proofs/Erdos/Erdos1213/Hegyvari.lean`)

**Source:** NEXT_TARGETS P1.

**Goal.** Prove: for any A ⊆ {1,...,n} with |A| > n/2, the set
{a + a' : a, a' ∈ A, a ≠ a'} covers all residues mod n. Pigeonhole
on interval-sum residues; zero new defs. Best payoff-per-effort in
the Erdős sweep. Fetch from `goof erdos fetch 1213`.

---

### Erdős #384 Ecklund
(new file `Proofs/Erdos/Erdos384/Ecklund.lean`)

**Source:** NEXT_TARGETS P1.

**Goal.** Prove: for n ≥ 2k, there exists a prime p ≤ n/2 such that
p | C(n,k), with the exception (n,k) = (7,3). Elementary via Bertrand.
Corrected statement already in sketch. Fetch from `goof erdos fetch 384`.
Read `Proofs/Erdos/Erdos175/KummerDigits.lean` for the Kummer machinery.

---

### Erdős #673 divisor ratios
(new file `Proofs/Erdos/Erdos673/DivisorRatios.lean`)

**Source:** NEXT_TARGETS P1.

**Goal.** Prove Tao's trivial route: τ(n)/4 ≤ G(n) ≤ τ(n) pointwise,
where G(n) = |{d | n : d ≤ √n and d' = n/d satisfies d'/d ∈ [1,2]}|.
Density half stays archived (sorry or Prop def). Fetch from
`goof erdos fetch 673`.

---

### Erdős #1062 no a|b, a|c
(new file `Proofs/Erdos/Erdos1062/NoDivisorTriple.lean`)

**Source:** NEXT_TARGETS P1.

**Goal.** Prove: the interval [m+1, 3m+2] gives f(n) ≥ ⌈2n/3⌉.
Small exact values decidable. Fetch from `goof erdos fetch 1062`.

---

### Erdős #1160 m = 2, 3 cases
(new file `Proofs/Erdos/Erdos1160/GnuCases.lean`)

**Source:** NEXT_TARGETS P1.

**Goal.** Prove `erdos_1160_m2` from landed gnu lemmas (GroupCount/Gnu).
Attempt m = 3 provable-with-work. Headline stays archived (gnu(64)=267
out of certified range). Fetch from `goof erdos fetch 1160`.

---

### A005153 Switkay parity
(see P15 above — same file, dispatched as a proof lane, not archive)

---

### Erdős #18 h(m) fragments
(new file `Proofs/Erdos/Erdos18/PracticalH.lean`)

**Source:** NEXT_TARGETS P1 + new-targets §6.

**Goal.** Define h(m) = largest n s.t. every integer in [1,n] is a sum
of distinct divisors of m. Prove h(n!) < n, certify the table
n = 3..11. Two sorrys in `E18.lean` now dischargeable from
`Practical.lean`. Fetch from `goof erdos fetch 18`.
