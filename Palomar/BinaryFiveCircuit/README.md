# Binary five-circuit ambient classification — recovered scouting note

## Status and exact candidate

This README recovers the prior scout’s completed draft. It is a candidate only—not a submission, acceptance, novelty, priority, approval, or human-review claim. The focused result is the declaration
`BilinearComplexity.BinaryFiveCircuitTheorem.finiteDimensional_ambient_fiveCircuit_classification`
in `Proofs/BilinearComplexity/BinaryFiveCircuitTheorem.lean`.

For arbitrary `U`, `V`, and `W`, the theorem assumes additive commutative groups, `F2`-module structures, decidable equality, and finite-dimensionality over `F2`. For `A B : State U V W`, it assumes

```lean
A.card = 2
B.card = 3
Disjoint A B
stateEvaluation A = stateEvaluation B
```

Its conclusion is

```lean
(∃! label : OrbitLabel, AmbientInOrbit label A B) ∧
  ∃ forward : MovePath (BinaryAmbientMoves.AllModeMove (U := U) (V := V) (W := W)) A B,
    ∃ reverse : MovePath (BinaryAmbientMoves.AllModeMove (U := U) (V := V) (W := W)) B A,
      forward.length ≤ 3 ∧ reverse.length ≤ 3 ∧
      forward.altitude ≤ 4 ∧ reverse.altitude ≤ 4 ∧
      AmbientPathFactorSpanConfined forward (A ∪ B) ∧
      AmbientPathFactorSpanConfined reverse (A ∪ B)
```

Thus it asserts a unique semantic `OrbitLabel`, bounded forward and reverse paths (at most three moves and altitude four), and factor-span confinement. It has no separate `Circuit` hypothesis or explicit `Circuit` conclusion. The cardinality, disjointness, and equal-evaluation assumptions are the stated contract for the normalized five-term relation. This candidate does **not** include the broader thirteen-orbit realization, profile-411 sharpness, or enumeration declarations.

## Definitions and contribution

`ExactSpanPresentation` (`BinaryAmbientNormalization.lean:40–55`) supplies a normalized profile and linear equivalences onto the three factor spans generated by `A ∪ B`. `normalizedExactRelation` (`:774–804`) and `exists_exactSpanPresentation` (`:822–843`) connect the ambient hypotheses to that normalized relation. In `BinaryFiveCircuitCompiler.lean`, `exactSpanCoordinateEmbedding` (`:39–60`), `compileBinaryFiveCircuit` (`:237–244`), the forward/reverse constructions (`:285–308`), transport bounds (`:310–392`), confinement results (`:394–424`), `AmbientInOrbit` (`:426–430`), and `exists_ambientFiveCircuitConclusion` (`:505–532`) package the normalized certificate into the ambient statement.

The compiler is presentation-explicit: it does not discover coordinates for arbitrary input. The universal existence theorem hides only the proof-side existence of a presentation. Confinement means projected factor spans lie in the spans generated by `A ∪ B`; it does not mean vertices remain in the finite endpoint set.

The possible contribution is an ambient, rather than merely coordinate-array, formulation combining semantic label uniqueness with explicit bidirectional reductions and altitude/span controls. Plausible audiences are binary linear algebra, finite-field tensor/rank-one circuit combinatorics, algebraic complexity, and reduction systems. Significance remains unresolved and no novelty is asserted.

## Attribution and evidence

The recovered scout found no confirmed external primary source or prior Lean formalization. `Plans/MatrixMultiplication.md:328–333` is only an internal provenance lead for an earlier thirteen-orbit computation, not independent evidence of completeness or novelty. Repository materials disclose AI assistance and state that proofs have not been rigorously vetted by a human; human review remains pending. The source declarations, normalized certificates, finite-action classification, relation enumeration, and coverage bridges are repository evidence, not independently audited mathematics.

The recovered bounded scan reported ordinary `decide` proofs in finite checking code and no `native_decide` in the two requested files or principal five-circuit/compiler chain, while noting repository-wide `native_decide` occurrences. This does not establish a trust audit: imported tables and computational checks must not be upgraded to independently verified standard-axiom proofs. Existing `#print axioms` commands do not constitute a completed audit; `native_decide`, `Lean.ofReduceBool`, `sorryAx`, custom axioms, and generated-table dependencies remain to be checked explicitly.

## Verification status, limitations, and next obligations

No fresh elaboration or build was run for this recovery. The prior record reports that a direct Lean invocation stopped at module resolution (`unknown module prefix 'BilinearComplexity'`) before theorem checking; therefore successful verification and axiom status must not be inferred. Next prover/package work is a source-path-correct targeted check of the theorem and compiler, followed by an explicit dependency and `#print axioms` audit under the pinned project environment. A future Palomar package must preserve the exact hypotheses and conclusion in a small Challenge/Solution bridge, add the required Comparator and provenance metadata, and disclose automation, AI authorship, license, review status, sources, and limitations.

Before any submission decision, perform a credible literature/prior-formalization search and human review of mathematical meaning and significance. Do not submit, register, or claim Palomar acceptance, global priority, fresh verification, or human approval from this scout.
