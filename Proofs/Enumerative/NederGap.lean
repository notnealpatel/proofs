/-
  LANE HALTED (2026-07-29, quota stop) — stub only, no content written.

  No dedicated card. E4 release lane: the Neder gap-12 anchor from the
  OEIS A083207 comments (consecutive Zumkeller numbers have gap ≤ 12,
  proved in literature — Rao–Peng, arXiv:0912.0052). The exact claim
  was to be pinned live from `oeis show A083207` before any Lean was
  written; that pinning never happened — do it first on re-dispatch.
  Writer was dispatched and killed minutes in; nothing beyond this stub
  exists.

  Ready for re-dispatch: Enumerative.IsZumkeller is in-tree, fully
  reviewed, sorry-free (DecidablePred for residue-class representatives;
  closure engine IsZumkeller.mul_of_coprime needs only coprimality).
-/
import Mathlib

set_option autoImplicit false
