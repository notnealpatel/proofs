/-
  LANE HALTED (2026-07-29, quota stop) — stub only, no content written.

  Card: Formalize/A007691-multiperfect-zumkeller.md. E4 release lane:
  statement-level multiperfect → Zumkeller claims; file-local
  multiplicative multiperfect predicate (σ(n) = k·n) with ground checks
  vs oeis show A007691. Writer was dispatched and killed while pulling
  OEIS ground truth; nothing beyond this stub exists.

  Ready for re-dispatch: Enumerative.IsZumkeller is in-tree, fully
  reviewed, sorry-free (kernel-decide capable; closure engine
  IsZumkeller.mul_of_coprime needs only coprimality).
-/
import Mathlib

set_option autoImplicit false
