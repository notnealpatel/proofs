/-
  LANE HALTED (2026-07-29, quota stop) — stub only, no content written.

  Card: Formalize/A083207-ianakiev-sigma-half.md. E4 release lane:
  statement-level sigma-half claims over the shared IsZumkeller layer.
  Writer was dispatched and killed minutes in; nothing beyond this stub
  exists.

  Ready for re-dispatch: Enumerative.IsZumkeller is in-tree, fully
  reviewed, sorry-free; the multiplicative half-σ form
  isZumkeller_iff_two_mul_sum_eq_sum_divisors (2 * ∑ = σ, no division)
  is the intended main tool.
-/
import Mathlib

set_option autoImplicit false
